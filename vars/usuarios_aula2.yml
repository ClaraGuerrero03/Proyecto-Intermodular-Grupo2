usuarios:
  - nombre: alumno11
    password: "{{ 'Cambiar1234!' | password_hash('sha512') }}"
    grupos: [alumnos]

  - nombre: alumno12
    password: "{{ 'Cambiar1234!' | password_hash('sha512') }}"
    grupos: [alumnos]

  - nombre: profesor
    password: "{{ 'Profesor123' | password_hash('sha512') }}"
    grupos: [alumnos, sudo]
